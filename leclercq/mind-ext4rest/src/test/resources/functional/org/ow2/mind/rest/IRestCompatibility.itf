/**
 * Information about the external component 
 */
#include "IMethods.idt"
#include "IData.idt"
 
interface org.ow2.mind.rest.IRestCompatibility {

  /// External interface name
  string getName();
  
  /// Implemented method list
  method_list* getMethods();
  
  /// method invocation
  void* invoke(char* method_name, param_list* parameters, data_list* body);
  
};
